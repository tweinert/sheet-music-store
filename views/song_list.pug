extends layout

block content
  h1= title
  if song_list.length
    ul
      each song in song_list
        li
          a(href=song.url) #{song.name}
          |  (#{song.composer.name})

  else
    p There are no songs.
